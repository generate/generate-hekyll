{
  "name": "<%= name %>",
  "description": "<%= alias %> prompt plugin for Enquirer.",
  "version": "0.1.0",
  "homepage": "https://github.com/<%= ask('owner') %>/<%= name %>",
  "author": "<%= ask('author.name') %> (https://github.com/<%= ask('author.username') %>)",
  "repository": "<%= ask('owner') %>/<%= name %>",
  "bugs": {
    "url": "https://github.com/<%= ask('owner') %>/<%= name %>/issues"
  },
  "license": "MIT",
  "files": [
    "index.js",
    "LICENSE"
  ],
  "main": "index.js",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "checkbox",
    "prompt"
  ]
}
